import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/research/publications/translations/')({
  component: () => <div>Hello /research/publications/translations/!</div>,
});
